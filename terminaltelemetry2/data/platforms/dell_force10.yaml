# Dell OS9 (Force10): session-only pack (no widget bindings yet). Paging and
# aliases follow the netmiko device type of the same name; not yet run
# against real gear -- set tested: true once it has been.
platform: dell_force10
title: Dell OS9 (Force10)
aliases: [force10, dell_os9, ftos]
tested: false
session:
  paging: terminal length 0
bindings: {}
