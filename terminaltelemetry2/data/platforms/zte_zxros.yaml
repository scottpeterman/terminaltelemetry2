# ZTE ZXROS: session-only pack (no widget bindings yet). Paging and
# aliases follow the netmiko device type of the same name; not yet run
# against real gear -- set tested: true once it has been.
platform: zte_zxros
title: ZTE ZXROS
aliases: [zxros, zte]
match:
  vendor: [zte]
tested: false
session:
  paging: terminal length 0
bindings: {}
