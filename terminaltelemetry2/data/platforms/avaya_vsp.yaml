# Extreme VOSS / VSP: session-only pack (no widget bindings yet). Paging and
# aliases follow the netmiko device type of the same name; not yet run
# against real gear -- set tested: true once it has been.
platform: avaya_vsp
title: Extreme VOSS / VSP
aliases: [extreme_vsp, voss, vsp]
tested: false
session:
  paging: terminal more disable
bindings: {}
