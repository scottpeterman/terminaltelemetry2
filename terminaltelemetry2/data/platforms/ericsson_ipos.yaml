# Ericsson IPOS: session-only pack (no widget bindings yet). Paging and
# aliases follow the netmiko device type of the same name; not yet run
# against real gear -- set tested: true once it has been.
platform: ericsson_ipos
title: Ericsson IPOS
aliases: [ipos]
match:
  vendor: [ericsson]
tested: false
session:
  paging: terminal length 0
bindings: {}
