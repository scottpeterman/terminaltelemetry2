# Ubiquiti EdgeRouter (EdgeOS): session-only pack (no widget bindings yet). Paging and
# aliases follow the netmiko device type of the same name; not yet run
# against real gear -- set tested: true once it has been.
platform: ubiquiti_edgerouter
title: Ubiquiti EdgeRouter (EdgeOS)
aliases: [edgeos, edgerouter]
tested: false
session:
  paging: set terminal length 0
bindings: {}
