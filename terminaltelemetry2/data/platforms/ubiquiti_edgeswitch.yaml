# Ubiquiti EdgeSwitch: session-only pack (no widget bindings yet). Paging and
# aliases follow the netmiko device type of the same name; not yet run
# against real gear -- set tested: true once it has been.
platform: ubiquiti_edgeswitch
title: Ubiquiti EdgeSwitch
aliases: [edgeswitch]
tested: false
session:
  paging: terminal length 0
  enable: enable
bindings: {}
