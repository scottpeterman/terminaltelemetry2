# Extreme EXOS: session-only pack (no widget bindings yet). Paging and
# aliases follow the netmiko device type of the same name; not yet run
# against real gear -- set tested: true once it has been.
platform: extreme_exos
title: Extreme EXOS
aliases: [exos, extreme]
tested: false
session:
  paging: disable clipaging
bindings: {}
