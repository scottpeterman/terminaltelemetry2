# Dell PowerConnect: session-only pack (no widget bindings yet). Paging and
# aliases follow the netmiko device type of the same name; not yet run
# against real gear -- set tested: true once it has been.
platform: dell_powerconnect
title: Dell PowerConnect
aliases: [powerconnect]
tested: false
session:
  paging: terminal datadump
bindings: {}
