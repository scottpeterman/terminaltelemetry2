# Huawei VRP: session-only pack (no widget bindings yet). Paging and
# aliases follow the netmiko device type of the same name; not yet run
# against real gear -- set tested: true once it has been.
platform: huawei_vrp
title: Huawei VRP
aliases: [vrp, huawei]
match:
  vendor: [huawei]
tested: false
session:
  paging: screen-length 0 temporary
bindings: {}
