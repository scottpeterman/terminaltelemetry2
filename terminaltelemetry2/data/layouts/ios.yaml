layout: ios
platforms: [cisco_ios, cisco_nxos]
terminal:
  position: left
  size: 0.4
rows:
  - [version, system, bgp_down, ospf_down, port_errdisabled]
  - [bgp_peers, ospf_neighbors]
  - [lldp_neighbors, intf_updown]
  - [intf_counters, ip_addresses]
